name: Release

on:
  workflow_dispatch:
  push:
    branches: [main]

# The reusable release workflow creates a GitHub Release; without contents: write
# the release step 403s and the rest of the pipeline never runs.
permissions:
  contents: write

jobs:
  release:
    uses: risa-labs-inc/BossConsole-Releases/.github/workflows/plugin-release.yml@main
    with:
      boss_plugin_api_version: 'latest'
    secrets:
      BOSS_STORE_PLUGIN_PUBLISH_KEY: ${{ secrets.BOSS_STORE_PLUGIN_PUBLISH_KEY }}
